{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "buildCommand": "cd relay-server && npm install",
    "startCommand": "cd relay-server && node index.js",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
