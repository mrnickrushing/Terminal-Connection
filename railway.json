{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nix"
  },
  "deploy": {
    "startCommand": "cd relay-server && npm install && npm start",
    "restartPolicyCondition": "on-failure",
    "restartPolicyMaxRetries": 5
  }
}
